Quality gate split verification (2026-06-15)

Blocker addressed:
- Reviewer 2 reported LOC-budget gaming in `packages/omo-codex/plugin/components/ulw-loop/src/quality-gate.ts`.
- Removed the formatter suppression and split responsibilities into:
  - `src/quality-gate-blockers.ts`: blocker evidence normalization, authorization blocker classification, blocker occurrence helpers.
  - `src/quality-gate-fields.ts`: primitive gate field parsing and fail-closed validation helpers.

Pure LOC after normal Biome formatting:
- `src/quality-gate.ts`: 239
- `src/quality-gate-blockers.ts`: 41
- `src/quality-gate-fields.ts`: 35

Anti-slop / suppression scan:
- `rg -n "biome-ignore|format: keep|@ts-ignore|@ts-expect-error|as any|as unknown|console\\.log" ...`
- Result: PASS, no matches.
- `placeholder` appears only in the intentional quality-gate placeholder rejection regex.

Commands and results:
- `npm exec -- biome check --write src/quality-gate.ts src/quality-gate-blockers.ts src/quality-gate-fields.ts`
  - Result: PASS, 3 files checked, no fixes remaining.
- `npm --prefix packages/omo-codex/plugin/components/ulw-loop run typecheck`
  - Result: PASS, `tsc --noEmit`.
- `bunx vitest run packages/omo-codex/plugin/components/ulw-loop/test/cli-checkpoint.test.ts packages/omo-codex/plugin/components/ulw-loop/test/checkpoint.test.ts packages/omo-codex/plugin/components/ulw-loop/test/quality-gate.test.ts packages/omo-codex/plugin/components/ulw-loop/test/quality-gate-roles.test.ts`
  - Result: PASS, 4 test files / 44 tests.
- `bun run --cwd packages/omo-codex/plugin check`
  - Result: PASS, component build plus plugin Node test suite, 217 tests.
- `bun run test:codex`
  - Result: PASS.
  - Summary: lsp-tools-mcp 25 files / 95 tests passed; Codex Bun suite 339 tests passed.
- Isolated local install:
  - `CODEX_HOME` pointed at a throwaway temp directory.
  - `node packages/omo-codex/scripts/install-local.mjs install`
  - Result: PASS, installed `omo@sisyphuslabs` 4.10.0 into the sandbox.
  - Real `~/.codex/config.toml` SHA before and after matched.
- tmux Codex smoke:
  - Ran `codex plugin list` under the sandbox `CODEX_HOME` inside tmux.
  - Result: PASS, `omo@sisyphuslabs` listed as `installed, enabled`, exit 0.
  - Cleanup: tmux session killed; throwaway CODEX_HOME removed.
- `git diff --check origin/dev..HEAD && git diff --check`
  - Result: PASS, no whitespace errors.
- Smart-rebase check:
  - `git fetch origin dev && git rev-list --left-right --count origin/dev...HEAD`
  - Result: `0 29`; branch is not behind `origin/dev`.

Review note:
- This fix does not add tests that merely prove a removed value stayed removed. It keeps existing behavior tests and only changes module boundaries to remove the LOC/formatter workaround.
