CodeGraph unavailable skip no-mutation evidence
Date: 2026-06-15
Worktree: /Users/yeongyu/local-workspaces/omo-wt/codegraph-omo-integration

Red proof before fix:
- `bun test test/hook.test.ts` from `packages/omo-codex/plugin/components/codegraph`
  failed because the unavailable worker path called `prepareWorkspace` and
  `ensureGitignored`, and a real temp workspace contained `.codegraph`.
- `bun test packages/omo-opencode/src/hooks/codegraph-bootstrap/codegraph-bootstrap.test.ts`
  failed because the unavailable OpenCode path called `prepareWorkspace`, and
  the real temp workspace contained `.codegraph`.
- `bun test packages/utils/src/codegraph-workspace.test.ts` failed because
  `ensureCodegraphGitignored()` returned true and created `.git/info/exclude`
  for a non-git temp workspace.

Fix summary:
- Codex worker now resolves/provisions CodeGraph before project workspace setup.
- OpenCode bootstrap now resolves/provisions CodeGraph before project workspace setup.
- `ensureCodegraphGitignored()` returns false when `.git` is absent, so it does
  not synthesize `.git/info/exclude` for non-git workspaces.

Commands and outputs:

1. Codex component typecheck
Command:
`bun run typecheck`
Working directory:
`packages/omo-codex/plugin/components/codegraph`
Output:
```
$ tsc --noEmit
```

2. Codex component tests
Command:
`bun test test/*.test.ts`
Working directory:
`packages/omo-codex/plugin/components/codegraph`
Output:
```
18 pass
0 fail
56 expect() calls
Ran 18 tests across 2 files.
```

3. OpenCode bootstrap + shared workspace focused tests
Command:
`bun test packages/omo-opencode/src/hooks/codegraph-bootstrap/codegraph-bootstrap.test.ts packages/utils/src/codegraph-workspace.test.ts`
Output:
```
16 pass
0 fail
36 expect() calls
Ran 16 tests across 2 files.
```

4. OpenCode package typecheck
Command:
`bun run typecheck`
Working directory:
`packages/omo-opencode`
Output:
```
$ tsgo --noEmit -p tsconfig.json
```

5. Utils package typecheck
Command:
`tsgo --noEmit -p packages/utils/tsconfig.json`
Output:
```
exit 0
```

6. Codex QA harness self-check
Command:
`bash .agents/skills/codex-qa/scripts/lib/common.sh --self-check`
Output:
```
PASS: dependencies present (codex node jq tmux)
PASS: codex binary -> /Users/yeongyu/.local/bin/codex
PASS: isolated CODEX_HOME auto-removed on exit
PASS: CODEX_HOME points inside sandbox, not ~/.codex
PASS: mock model serves Responses SSE
PASS: real ~/.codex/config.toml unchanged
PASS: common.sh self-check
```

7. Codex QA hook-unit self-test
Command:
`bash .agents/skills/codex-qa/scripts/hook-unit-probe.sh --self-test`
Output:
```
installing local omo into isolated CODEX_HOME ...
PASS: real ~/.codex/config.toml unchanged
PASS: ultrawork UserPromptSubmit injected <ultrawork-mode> on an ulw prompt
```

8. OpenCode QA harness self-check
Command:
`bash .agents/skills/opencode-qa/scripts/lib/common.sh --self-check`
Output:
```
PASS: dependencies present (opencode sqlite3 curl jq tmux)
PASS: oqa_db_path -> /Users/yeongyu/.local/share/opencode/opencode.db
PASS: isolated XDG sandbox auto-removed on exit
PASS: isolated HOME points inside sandbox
PASS: common.sh self-check
```

9. OpenCode SSE QA self-test
Command:
`bash .agents/skills/opencode-qa/scripts/sse-hook-probe.sh --self-test`
Output:
```
first matching event: {"type":"server.connected"}
PASS: SSE /event opened and delivered server.connected
```

10. Built Codex wrapper smoke, non-git temp workspace, missing binary, auto-provision disabled
Command:
`HOME="$home_dir" OMO_CODEGRAPH_AUTO_PROVISION=0 OMO_CODEGRAPH_BIN="$missing" OMO_CODEGRAPH_SESSION_START_CWD="$workspace" node packages/omo-codex/plugin/components/codegraph/dist/cli.js hook session-start-worker`
Output:
```
codegraph_exists=no
git_exclude_exists=no
outcome={"error":"codegraph binary unavailable and auto_provision is disabled","source":"env","action":"skipped-unavailable"}
```

11. OpenCode hook smoke, non-git temp workspace, missing binary, auto-provision disabled
Command:
`bun --eval '<createCodegraphBootstrapHook smoke with resolveCommand returning missing binary>'`
Output:
```
codegraph_exists=no
git_exclude_exists=no
events=["[codegraph-bootstrap] CodeGraph unavailable; skipping bootstrap {...}"]
```

Build artifact:
- Ran `bun run build` in `packages/omo-codex/plugin/components/codegraph`;
  `dist/cli.js` was regenerated from the updated hook source.

Notes:
- The worktree also contained unrelated CodeGraph diffs (Windows provision,
  env resolution, and MCP tests). They were not part of this fix and were left
  unstaged.
