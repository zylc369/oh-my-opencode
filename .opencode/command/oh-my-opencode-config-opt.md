---
description: 优化oh-my-opencode的配置，配置路径`~/.config/opencode/oh-my-opencode.json`
---

`oh-my-opencode`下面简称`omo`。

1. 当前是omo的源码目录，请根据omo源码目录下的docs中的英文文档（即，文件后缀不是`.zh-cn.md`的文档）以及源码，帮我分析配置文件中的`agents`和`categories`下面的模型配置是否有优化空间，模型相关的字段有：model、variant。注意：不要增删`agents`和`categories`下面的项，只是优化。
2. 使用者不可能都有各种类型的LLM会员，所以优化前询问使用者有哪些模型能用。