Fix: Windows Codex CodeGraph wrapper install_dir lookup

Scope:
- packages/omo-codex/plugin/components/codegraph/src/hook.ts
- packages/omo-codex/plugin/components/codegraph/src/serve.ts
- packages/omo-codex/plugin/components/codegraph/test/hook.test.ts
- packages/omo-codex/plugin/components/codegraph/test/serve.test.ts
- packages/omo-codex/plugin/components/codegraph/dist/cli.js
- packages/omo-codex/plugin/components/codegraph/dist/serve.js

Behavior:
- Windows install_dir lookup now resolves bin/codegraph.cmd, matching the manifest/provision resolver.
- Added Windows simulation coverage for runCodegraphSessionStartWorker and runCodegraphServe.

Red check before source fix:

Command:
cd packages/omo-codex/plugin/components/codegraph && bun test test/*.test.ts

Result:
- Exit code: 1
- 17 pass, 2 fail
- Failing tests:
  - runCodegraphServe > #given Windows Codex SOT install_dir has codegraph.cmd #when serving MCP #then it resolves there and exports CODEGRAPH_INSTALL_DIR
    Expected exitCode 0, received 1.
  - CodeGraph SessionStart hook > #given Windows install_dir has codegraph.cmd #when worker resolves provisioned CodeGraph #then it uses the cmd shim
    Expected action "initialized", received "failed".

Green verification:

Command:
cd packages/omo-codex/plugin/components/codegraph && bun test test/*.test.ts

Output:
bun test v1.3.14 (0d9b296a)

test/serve.test.ts:
(pass) runCodegraphServe > #given CodeGraph is unresolved #when serving MCP #then exits non-zero with a one-line skip hint
(pass) runCodegraphServe > #given CodeGraph resolves #when serving MCP #then execs codegraph serve --mcp with inherited stdio and telemetry disabled
(pass) runCodegraphServe > #given OMO_CODEGRAPH_BIN points at a missing path #when serving MCP #then exits before spawn
(pass) runCodegraphServe > #given Codex SOT disables CodeGraph #when serving MCP #then exits non-zero with a disabled hint
(pass) runCodegraphServe > #given Windows Codex SOT install_dir has codegraph.cmd #when serving MCP #then it resolves there and exports CODEGRAPH_INSTALL_DIR
(pass) runCodegraphServe > #given built serve entry #when invoked with a fake CodeGraph binary #then it runs serve mcp exactly once
(pass) runCodegraphServe > #given built cli entry #when invoked with a fake CodeGraph binary #then it runs serve mcp exactly once

test/hook.test.ts:
(pass) CodeGraph SessionStart hook > #given hook session-start cli args #when invoked with empty JSON input #then it emits valid JSON and exits zero
(pass) CodeGraph SessionStart hook > #given CodeGraph is disabled by Codex SOT config #when SessionStart fires #then it skips without spawning
(pass) CodeGraph SessionStart hook > #given HOME OMO config disables Codex CodeGraph #when SessionStart fires #then it skips without spawning
(pass) CodeGraph SessionStart hook > #given project Codex SOT disables global CodeGraph enablement #when SessionStart fires #then project config wins
(pass) CodeGraph SessionStart hook > #given env disables CodeGraph over SOT enablement #when SessionStart fires #then it skips without spawning
(pass) CodeGraph SessionStart hook > #given CodeGraph is enabled #when SessionStart fires #then it detaches a background worker immediately
(pass) CodeGraph SessionStart hook > #given CodeGraph cannot be resolved or provisioned #when worker runs #then it logs a graceful skip
(pass) CodeGraph SessionStart hook > #given CodeGraph is unavailable and auto provisioning is disabled #when worker runs #then it leaves the project untouched
(pass) CodeGraph SessionStart hook > #given Windows install_dir has codegraph.cmd #when worker resolves provisioned CodeGraph #then it uses the cmd shim
(pass) CodeGraph SessionStart hook > #given resolved CodeGraph status #when worker runs #then it runs status before init or sync
(pass) CodeGraph SessionStart hook > #given malformed hook input #when SessionStart fires #then it still emits JSON and exits zero
(pass) CodeGraph SessionStart hook > #given plugin hook config #when inspected #then CodeGraph is registered after bootstrap SessionStart

19 pass
0 fail
60 expect() calls

Command:
cd packages/omo-codex/plugin/components/codegraph && bun run typecheck

Output:
$ tsc --noEmit

Command:
cd packages/omo-codex/plugin/components/codegraph && bun run build

Output:
$ bun build src/serve.ts --target node --format esm --outfile dist/serve.js && bun build src/cli.ts --target node --format esm --outfile dist/cli.js
Bundled 17 modules
serve.js  48.64 KB  (entry point)
Bundled 22 modules
cli.js  72.87 KB  (entry point)

Command:
cd packages/omo-codex/plugin/components/codegraph && rg -n "codegraph\.(cmd|exe)" dist/cli.js dist/serve.js src/hook.ts src/serve.ts test/hook.test.ts test/serve.test.ts

Key output:
src/serve.ts:109: process.platform === "win32" ? "codegraph.cmd" : "codegraph"
src/hook.ts:267: process.platform === "win32" ? "codegraph.cmd" : "codegraph"
dist/serve.js:1452: process.platform === "win32" ? "codegraph.cmd" : "codegraph"
dist/cli.js:1874: process.platform === "win32" ? "codegraph.cmd" : "codegraph"
dist/cli.js:1978: process.platform === "win32" ? "codegraph.cmd" : "codegraph"

Command:
cd packages/omo-codex/plugin/components/codegraph && rg -n "codegraph\.exe" dist/cli.js dist/serve.js src/hook.ts src/serve.ts test/hook.test.ts test/serve.test.ts

Result:
- Exit code: 1
- No matches.
