# Codex Workflow Selector

Opt-in LazyCodex `UserPromptSubmit` selector for routing normal prompts toward existing workflows.

Enable with `OMO_CODEX_AUTO_WORKFLOW=1`. When disabled, the hook exits silently.
