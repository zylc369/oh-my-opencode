Failure source:
- GitHub Actions run 28217650284, job 83591984908, `test (windows-latest)`.
- Failed test: `ensureCodegraphProvisioned > extracts a verified release archive and installs bin/codegraph`.
- Observed failure: the test timed out after Bun's 5000 ms default, at 5016 ms.
- Same fresh run had ubuntu and macos `test` green, so the remaining blocker was Windows-specific timing.

Fix:
- Added an explicit 15000 ms timeout to the real release-archive extraction test in `packages/utils/src/codegraph-provision.test.ts`.
- Production code and assertions were unchanged.

Focused verification:
- `bun test packages/utils/src/codegraph-provision.test.ts -t "extracts a verified release archive and installs bin/codegraph"`
  - 1 pass, 0 fail.
- `bun test packages/utils/src/codegraph-provision.test.ts`
  - 7 pass, 0 fail.
- `bun run --cwd packages/utils typecheck`
  - `tsgo --noEmit -p tsconfig.json`, exit 0.
- `bun run /Users/yeongyu/.codex/plugins/cache/sisyphuslabs/omo/4.13.0/skills/programming/scripts/typescript/check-no-excuse-rules.ts packages/utils/src/codegraph-provision.test.ts`
  - No violations in 1 file.
- `bun test packages/utils/src/*.test.ts packages/utils/src/**/*.test.ts`
  - 365 pass, 0 fail.

Omitted:
- `bun run --cwd packages/utils test` was not used as the final package gate because it changes `process.cwd()` to `packages/utils` and exposes an unrelated existing runtime-shim path assumption. The equivalent repo-root invocation above matches CI's root `bun test` shape and passed.
