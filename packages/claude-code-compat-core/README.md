# claude-code-compat-core

Consumer contract: harness adapters such as `omocc` may use this package to discover and translate Claude Code plugins, commands, agents, skills, and MCP configs without depending on the OpenCode adapter.
